# Ch.6 — The Ruins That Laugh (Africa) — promotion checklist

> Generated by `npm run scaffold -- ch6` (ADR-047). The forge built the
> boring 60%; this chapter session spends itself on the SOUL. Promotion is a
> human act (Prime Law 1) — drafts under `src/data/drafts/ch6/` are dev-only.

## The chapter at a glance (from the manifest)

- **Region / travel-in:** Africa · biplane
- **Target end level:** 30 · **roster band:** `ch6`
- **Ember #6**
- **Dungeon:** The Laughing Ruins → the Sphinx’s chin (grammar `laughing_ruins`)
- **Boss:** The Laughing Sphinx (`laughing_sphinx`, 2300 HP, template `riddle`)
- **Settlements:** zanzibel (city)

## Promote the draft tree

- [ ] **Dungeon** — walk `CH6_DUNGEON_RECIPE` in the LEVELKIT LAB; reskin with
      hand art (ADR-020), name the rooms in §A11 voice, place the boss-trigger door.
- [ ] **Roster** — `CH6_ROSTER` (forged): pick/redraw each face in the Sprite Lab,
      write §A11 names + death lines, expand to the §A7 twenty, then move the
      stripped `EnemyDef`s into `src/data/enemies.ts` and extend the §A7 manifest.
- [ ] **Boss `laughing_sphinx`** — rewrite its `*_draft` dialogue ids into real §A11 lines,
      register `FORM_ART` for any suffixed form, move it into `src/data/bosses.ts`.
- [ ] **Settlement `zanzibel`** (city) — build the landmarks, write the NPC
      dialogue + shops, drop the role-tagged draft slots, tag `settlement: 'city'`.
- [ ] **Quests** — author the five §A10 quests (the named core: `stones_that_speak`, `watering_hole_convoy`);
      each adds/strengthens a finale CALLER. Wire them into `src/data/quests.ts`.

## Land the chapter (the S14c flip)

- [ ] Flip `CHAPTER_MANIFESTS['6'].status` to `'shipped'` and fill in the LIVE
      `maps` + `dungeon.maps` + `quests` — the validator's live assertions switch on.
- [ ] `npm test` (tsc + validate + vitest) green; append the chapter's ADR.
