# Ch.7 — The Cobra's Palace (India) — promotion checklist

> Generated by `npm run scaffold -- ch7` (ADR-047). The forge built the
> boring 60%; this chapter session spends itself on the SOUL. Promotion is a
> human act (Prime Law 1) — drafts under `src/data/drafts/ch7/` are dev-only.

## The chapter at a glance (from the manifest)

- **Region / travel-in:** India · train
- **Target end level:** 35 · **roster band:** `ch7`
- **Ember #7**
- **Dungeon:** The Night Train → palace throne (grammar `night_train`)
- **Boss:** Cobra Raja (`cobra_raja`, 3200 HP, template `thresholdHeal`)
- **Settlements:** chandrapore (city)

## Promote the draft tree

- [ ] **Dungeon** — walk `CH7_DUNGEON_RECIPE` in the LEVELKIT LAB; reskin with
      hand art (ADR-020), name the rooms in §A11 voice, place the boss-trigger door.
- [ ] **Roster** — `CH7_ROSTER` (forged): pick/redraw each face in the Sprite Lab,
      write §A11 names + death lines, expand to the §A7 twenty, then move the
      stripped `EnemyDef`s into `src/data/enemies.ts` and extend the §A7 manifest.
- [ ] **Boss `cobra_raja`** — rewrite its `*_draft` dialogue ids into real §A11 lines,
      register `FORM_ART` for any suffixed form, move it into `src/data/bosses.ts`.
- [ ] **Settlement `chandrapore`** (city) — build the landmarks, write the NPC
      dialogue + shops, drop the role-tagged draft slots, tag `settlement: 'city'`.
- [ ] **Quests** — author the five §A10 quests (the named core: `seven_spices`, `monkey_who_stole_tuesday`);
      each adds/strengthens a finale CALLER. Wire them into `src/data/quests.ts`.

## Land the chapter (the S14c flip)

- [ ] Flip `CHAPTER_MANIFESTS['7'].status` to `'shipped'` and fill in the LIVE
      `maps` + `dungeon.maps` + `quests` — the validator's live assertions switch on.
- [ ] `npm test` (tsc + validate + vitest) green; append the chapter's ADR.
