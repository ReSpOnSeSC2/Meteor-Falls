{
  "name": "meteor-falls",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "METEOR FALLS — an EarthBound-style RPG. Four kids, eight Embers, one planet at the end of the road.",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && npm run validate && vite build",
    "preview": "vite preview --host",
    "validate": "vite-node tools/content-validate.ts",
    "test": "npm run validate && vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.87.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
