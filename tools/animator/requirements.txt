# Cadence — fully offline character-animation tool (no paid APIs).
# Backend + engine + CLI + tests.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pillow==11.1.0
numpy==2.2.1
python-multipart==0.0.20
pytest==8.3.4
